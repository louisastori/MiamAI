server:
  port: 8080

spring:
  application:
    name: miamai-api

miamai:
  integrations:
    ai-provider: mock
    leclerc-provider: mock
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173,http://localhost:8080
